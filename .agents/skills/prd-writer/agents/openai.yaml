interface:
  display_name: "PRD Writer"
  short_description: "Write evidence-backed implementation-ready specs"
  default_prompt: "Use $prd-writer to write or revise an implementation-ready SPEC and task list against the current repository."
